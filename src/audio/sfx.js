(()=>{'use strict';let ctx=null,enabled=localStorage.getItem('notblock_sound')!=='off';function ensure(){if(!enabled)return null;if(!ctx){const A=window.AudioContext||window.webkitAudioContext;if(!A)return null;ctx=new A}if(ctx.state==='suspended')ctx.resume();return ctx}function tone(freq=440,duration=.06,type='sine',gain=.045,delay=0,endFreq=null){const c=ensure();if(!c)return;const t=c.currentTime+delay,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);if(endFreq)o.frequency.exponentialRampToValueAtTime(Math.max(20,endFreq),t+duration);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(gain,t+.008);g.gain.exponentialRampToValueAtTime(.0001,t+duration);o.connect(g).connect(c.destination);o.start(t);o.stop(t+duration+.02)}function noise(duration=.08,gain=.025,delay=0){const c=ensure();if(!c)return;const len=Math.max(1,(c.sampleRate*duration)|0),b=c.createBuffer(1,len,c.sampleRate),d=b.getChannelData(0);for(let i=0;i<len;i++)d[i]=(Math.random()*2-1)*(1-i/len);const s=c.createBufferSource(),g=c.createGain(),t=c.currentTime+delay;s.buffer=b;g.gain.setValueAtTime(gain,t);g.gain.exponentialRampToValueAtTime(.0001,t+duration);s.connect(g).connect(c.destination);s.start(t)}const api={unlock:ensure,isEnabled:()=>enabled,setEnabled(v){enabled=!!v;localStorage.setItem('notblock_sound',enabled?'on':'off');if(enabled){ensure();tone(660,.045,'sine',.025)}return enabled},toggle(){return api.setEnabled(!enabled)},pick(){tone(420,.045,'sine',.022,0,560)},place(){tone(250,.055,'triangle',.035,0,180);tone(520,.035,'sine',.018,.018)},invalid(){tone(135,.065,'triangle',.018,0,105)},clear(lines=1){noise(.07,.022);const n=Math.min(4,Math.max(1,lines));for(let i=0;i<n;i++)tone(520+i*145,.095,'sine',.04,i*.035,700+i*160)},combo(lines=2){tone(620,.08,'triangle',.04);tone(830,.09,'triangle',.045,.055);if(lines>2)tone(1100,.12,'sine',.04,.11)},gameOver(){tone(330,.16,'triangle',.035,0,220);tone(220,.22,'triangle',.03,.13,110)},restart(){tone(360,.05,'sine',.02);tone(540,.07,'sine',.025,.045)}};window.NotBlockAudio=api;document.addEventListener('pointerdown',ensure,{once:true,passive:true});})();